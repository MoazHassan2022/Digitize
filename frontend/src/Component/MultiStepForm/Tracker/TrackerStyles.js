import { makeStyles } from "@mui/styles";

const useStyle = makeStyles({
    TrackerContainer:{
        overflowY:"scroll",
        color:"white",
        maxHeight:"60vh",
        padding:"2rem",
    }
  
});
  
  export default useStyle;
  